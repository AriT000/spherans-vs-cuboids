using UnityEngine;
using UnityEngine.Events;

public class GunController : MonoBehaviour
{

    private UnityEvent OnGunShoot;
    private float currentCooldown;
    private IWeapon playerWeapon;


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
